% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_split.R
\name{split_data}
\alias{split_data}
\title{Split Data for Multiple Datasets}
\usage{
split_data(
  df_list,
  target_col,
  train_ratio = 0.75,
  maxiter = 2,
  ntree = 20,
  scale_cols = NULL,
  seed = 2024
)
}
\arguments{
\item{df_list}{A named list of data frames to process.}

\item{target_col}{A character string specifying the target column name (assumed to be consistent across all datasets).}

\item{train_ratio}{Proportion of data to use for training (for \code{split_train}). Default is 0.75.}

\item{maxiter}{Number of iterations for imputation (for \code{split_train}). Default is 2.}

\item{ntree}{Number of trees for imputation (for \code{split_train}). Default is 20.}

\item{scale_cols}{Columns to scale (for \code{split_train}). If NULL, numeric columns are scaled. Default is NULL.}

\item{seed}{Random seed for reproducibility. Default is 2024.}
}
\description{
This function processes a list of datasets, applying different splitting strategies based on the dataset name.
For \code{"Bank Fraud Dataset"}, it uses \code{split_train_test_by_month} with predefined parameters.
For all other datasets, it uses \code{split_train}.
}
